DELETE THIS FILE AFTER API IS HOOKED UP !! -MAGNUS

Get the URL from Member 1 (e.g., https://YOUR-FN.azurewebsites.net/api/insights).
In script.js, set that URL in fetchInsights().
Replace the refresh handler with the API call:

refreshBtn?.addEventListener("click", fetchInsights);3